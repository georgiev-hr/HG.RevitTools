<Window x:Class="HG.RevitTools.Views.ElementSelector"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Items"
        Height="500"
        Width="600">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label x:Name="HeaderLabel"
       Grid.Row="0"
       Content="Select Items"
       HorizontalAlignment="Center"
       FontSize="20"
       Margin="0,10"/>

        <ScrollViewer Grid.Row="1" Margin="20" VerticalScrollBarVisibility="Auto">
            <ItemsControl Name="ItemsList" Background="#FFD9D9D9">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding DisplayName}"
                                  IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                  Margin="5"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,10">
            <Button Content="Select" Width="90" Margin="10,0" Click="Select"/>
            <Button Content="Select All" Width="90" Margin="10,0" Click="SelectAll"/>
            <Button Content="Cancel" Width="90" Margin="10,0" Click="Cancel"/>
        </StackPanel>
    </Grid>
</Window>